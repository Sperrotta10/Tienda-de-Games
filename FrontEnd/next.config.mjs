/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["i.3djuegos.com","example.com"], // Agrega aquí los dominios permitidos
    },
  };
export default nextConfig;
